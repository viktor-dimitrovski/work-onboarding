# Ops scripts configuration (do not commit your real ops.env)

############################################
# SSH to VPS
############################################
OPS_SSH_HOST=your.vps.ip.or.host
OPS_SSH_USER=deploy
OPS_SSH_PORT=22
# Optional key file (OpenSSH format). Leave empty to use agent/default key.
OPS_SSH_KEYFILE=

############################################
# VPS paths / services
############################################
# Repo root on the VPS (this repo is expected to exist there)
OPS_REMOTE_APP_DIR=/opt/my-onboarding

# systemd services (created by deploy/ubuntu scripts)
OPS_BACKEND_SERVICE=onboarding-backend
OPS_FRONTEND_SERVICE=onboarding-frontend

############################################
# Local database connection (source for backup)
############################################
# Prefer a plain postgres URL. If you paste SQLAlchemy URL with +psycopg, the script will normalize it.
OPS_LOCAL_DATABASE_URL=postgresql://onboarding_app:onboarding_app_dev_password@127.0.0.1:5432/onboarding

############################################
# Remote backup directory (on VPS)
############################################
OPS_REMOTE_BACKUP_DIR=/var/backups/onboarding/db

############################################
# Optional deploy knobs
############################################
OPS_GIT_BRANCH=main

